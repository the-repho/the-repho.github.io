<16 2 | cos(r+pi 3)*127+128<<8 3*2)*127+128).tostring(16); g2d.fill(); q[0]="q[1];" q[1]="{x:" k, y: n}; } function line(p){ t="p" + (random() * - 1.1) f; return (t> height || t < 0) ? line(p) : t;
  }

  document.onclick = redraw;
  document.ontouchstart = redraw;
  redraw();
}();
</16>